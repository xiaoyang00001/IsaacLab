# G1 机器人行走控制说明

## 当前状态

文件 [locomanipulation_g1_env_cfg.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/locomanipulation_g1_env_cfg.py) 第 61–63 行：

```python
FIXED_G1_29DOF_CFG = G1_29DOF_CFG.copy()
FIXED_G1_29DOF_CFG.spawn.articulation_props.fix_root_link = True
FIXED_G1_29DOF_CFG.spawn.rigid_props.disable_gravity = True
```

`robot` 与 `remote_robot` 两个机器人的根关节被**固定**（`fix_root_link = True`），因此本身无法物理行走。

第三个机器人 **`walker_robot`** 在同一环境内独立存在，并由 `AutoWalkAction` 通过"模拟骨骼数据"驱动全身做行走动画。详见下方 [方案三](#方案三模拟全身骨骼数据驱动walker_robot当前已启用)。

---

## 行走控制方案概览

| 方案 | 使用对象 | 是否启用 | 备注 |
|------|----------|----------|------|
| 一：滑步平移（仅根节点） | robot / remote_robot | 部分可用 | `_apply_absolute_root_pose` 已实现，腿不动 |
| 二：完整物理行走（ONNX 策略） | robot | 未启用 | 需提供 `.onnx`/`.pt` 行走策略模型 |
| **三：模拟骨骼数据驱动（全身动画）** | **walker_robot** | **✅ 已启用** | **腿+腰+手臂+手；含摆臂** |

---

## 方案一：直接平移根节点（滑步，无腿部动画）

这是 [mdp/actions.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/mdp/actions.py) 中 `AgileBasedLowerBodyAction` 的**当前实际行为**。

`_apply_absolute_root_pose()` 通过 `write_root_state_to_sim` 直接写入根节点位姿，命令格式为：

```
actions = [vx, vy, wz, hip_height]
```

| 参数 | 含义 |
|------|------|
| `vx` | 前后速度 |
| `vy` | 左右速度 |
| `wz` | 转向角速度 |
| `hip_height` | 髋关节高度（默认 0.72 m） |

**效果**：根节点平移/转向，但腿部维持站立姿（"滑步"）。

---

## 方案二：完整物理行走（需要预训练行走策略模型）

使用 `AgileBasedLowerBodyAction` + ONNX/TorchScript 行走策略，腿部会产生真实的行走动画。

### 步骤 1：解除根关节固定

```python
FIXED_G1_29DOF_CFG = G1_29DOF_CFG.copy()
# 删除 fix_root_link / disable_gravity 设置
```

### 步骤 2：在 ActionsCfg 中启用下肢动作

```python
lower_body = AgileBasedLowerBodyActionCfg(
    asset_name="robot",
    joint_names=[
        "left_hip_pitch_joint", "left_hip_roll_joint", "left_hip_yaw_joint",
        "left_knee_joint", "left_ankle_pitch_joint", "left_ankle_roll_joint",
        "right_hip_pitch_joint", "right_hip_roll_joint", "right_hip_yaw_joint",
        "right_knee_joint", "right_ankle_pitch_joint", "right_ankle_roll_joint",
    ],
    obs_group_name="lower_body_policy",
    policy_path="<ONNX或TorchScript行走策略文件路径>",
)
```

### 步骤 3：配置行走命令来源（选其一）

**A：VR 头显** — `G1LowerBodyStandingRetargeterCfg`
**B：手柄摇杆** — `G1LowerBodyStandingMotionControllerRetargeterCfg`

### 瓶颈

`policy_path` 当前为 `MISSING`，必须填入实际路径。仓库中已有 `assets/model/policy.onnx` 与 `policy1.onnx` 可作为候选，但需要与 `AgileTeacherPolicyObservationsCfg`（14 帧 × 65 维观测）的输入维度匹配后才能启用。

---

## 方案三：模拟全身骨骼数据驱动（walker_robot — 当前已启用）

第三个机器人 **`walker_robot`** 已加入场景，点击 Play 后会沿其朝向（+Y）匀速行走，并由内部生成的"骨骼姿态"驱动全身关节做协调动作（步态 + 摆臂 + 腰扭 + 放松握手）。

### 行走机制："滑步" + 关节动画

当前实现采用**动画驱动**方式，而非真实物理行走：

1. **根节点直接平移**
   - 每帧通过 `write_root_state_to_sim` 直接修改根节点世界坐标
   - 速度恒定（0.3 m/s），沿当前朝向移动
   - **没有物理接触**：根节点被"传送"，不依赖地面反作用力

2. **关节动画同步播放**
   - 腿部做步态动作（髋摆、膝弯、踝补偿）
   - 手臂反向摆动
   - 腰部扭转和侧倾
   - 髋部旋转
   - 躯干竖向起伏（body bob）

**特点**：
- ✅ 稳定，不会摔倒
- ✅ 动画流畅，全身协调
- ⚠️ 脚不接触地面（腿在空中做动作）
- ⚠️ 不是真实物理行走

**设计目标**：展示全身协调动画，模拟骨骼捕捉数据重定向的效果，而非物理仿真行走。

### 概念上的数据流

```text
walking_phase(t)
   ↓
_sample_skeleton_pose(phase)         ← 等价于 retarget(mocap_data, robot_skeleton)
   ↓
全身关节目标角度（dict）
   ↓
set_joint_position_target(...)
```

`_sample_skeleton_pose` 用解析公式合成与 walking phase 同步的全身姿态，模拟一个本地 mocap 流。**结构与真实 mocap 重定向函数一致**，后续可替换为读取 ZMQ/BVH/SMPL 数据流。

### 驱动覆盖范围

| 部位 | 关节数 | 行为 |
|------|--------|------|
| 腿部 | 12 | 左右 180° 反相 CPG 步态：髋俯仰摆腿、膝弯曲（仅摆动相）、踝反向补偿 |
| 手臂 | 14 | **反向摆臂**：肩 pitch 与同侧腿前后位置反相；肘随摆动微弯 |
| 腰部 | 3 | **yaw 扭转**：小幅反向扭转，增加自然感<br>**roll 侧倾**：模拟重心转移，支撑腿侧身体倾斜 |
| 髋部 | 2 | **yaw 旋转**：左右髋关节协同旋转，产生骨盆旋转效果 |
| 躯干 | - | **竖向起伏**：双腿支撑时最低，单腿支撑时最高（1.5cm 幅度） |
| 手部 | ≤14 | 恒定轻微卷曲（放松握拳）；不存在则自动跳过 |

### 自然感优化

为消除"幽灵感"，实现了以下关键改进：

1. **躯干竖向起伏（Body Bob）**
   - 双腿支撑时身体最低，单腿支撑时最高
   - 频率是步态的 2 倍（每步两次起伏）
   - 默认幅度 1.5cm

2. **膝关节相位修正**
   - 膝关节在腿前摆中段（mid-swing）弯曲最大
   - 符合真实人类行走的运动规律

3. **身体侧向倾摆**
   - 左腿支撑时身体向左倾，右腿支撑时向右倾
   - 模拟重心转移

4. **骨盆旋转**
   - 髋部 yaw 与腰部协同旋转
   - 增强行走的自然感

### 摆臂相位约定

```
θ_L = phase            (左腿相位)
θ_R = phase + π        (右腿相位，180° 偏移)

左腿前后:  hip_pitch_L = default + A_hip * sin(θ_L)
左肩前后:  shoulder_pitch_L = default + A_arm * sin(θ_L)   ← 与同侧腿"位置"反相
右腿/右肩类似（用 θ_R）。
```

物理上：左腿向前迈进时，**右臂前摆**（保持角动量守恒）—这是人类行走的天然规律。

### 关键代码

#### `AutoWalkAction`（[mdp/actions.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/mdp/actions.py)）

```python
class AutoWalkAction(ActionTerm):
    def _sample_skeleton_pose(self, phase: torch.Tensor) -> torch.Tensor:
        """模拟骨骼捕捉数据 → 全身关节目标。"""
        targets = self._default_joint_pos.clone()
        # LEGS: CPG 步态（膝关节相位已修正）
        # ARMS: 反向摆动
        # WAIST: yaw 扭转 + roll 侧倾
        # HIP YAW: 骨盆旋转
        # HANDS: 恒定卷曲
        return targets

    def process_actions(self, actions):
        # 1. phase += 2π·f·dt
        # 2. 根节点沿朝向平移（直接修改世界坐标）
        # 3. 添加躯干竖向起伏（body bob）
        # 4. self._processed_actions = self._sample_skeleton_pose(phase)
```

容错：`_resolve_joints` 对缺失关节**自动跳过并打印警告**，兼容 G1 是否携带 inspire hand 等变体。

#### `AutoWalkActionCfg`（[configs/action_cfg.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/configs/action_cfg.py)）

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `forward_speed` | 0.3 m/s | 行走线速度 |
| `walk_frequency` | 0.8 Hz | 步态周期频率 |
| `body_bob_amplitude` | 0.015 m | 躯干竖向起伏幅度（1.5cm） |
| `hip_pitch_amplitude` | 0.25 rad | 髋俯仰摆动幅度 |
| `knee_amplitude` | 0.30 rad | 膝弯曲幅度 |
| `ankle_pitch_amplitude` | 0.12 rad | 踝俯仰补偿 |
| `arm_swing_amplitude` | 0.35 rad | 肩 pitch 摆动幅度 |
| `elbow_bend_amplitude` | 0.15 rad | 肘弯曲幅度 |
| `waist_yaw_amplitude` | 0.06 rad | 腰 yaw 扭转幅度 |
| `waist_roll_amplitude` | 0.05 rad | 腰 roll 侧倾幅度（重心转移） |
| `hip_yaw_amplitude` | 0.03 rad | 髋 yaw 旋转幅度（骨盆旋转） |
| `hand_curl_amount` | 0.10 rad | 手指卷曲量（0 关闭） |

#### Walker 机器人配置（[locomanipulation_g1_env_cfg.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/locomanipulation_g1_env_cfg.py)）

```python
WALKER_G1_29DOF_CFG = FIXED_G1_29DOF_CFG.copy()
WALKER_G1_29DOF_CFG.init_state.pos = (-2.0, 0.0, 0.75)
WALKER_G1_29DOF_CFG.init_state.rot = (1.0, 0.0, 0.0, 0.0)

# Scene
walker_robot: ArticulationCfg = WALKER_G1_29DOF_CFG.replace(prim_path="{ENV_REGEX_NS}/WalkerRobot")

# Actions
walker_skeletal_walk = AutoWalkActionCfg(
    asset_name="walker_robot",
    joint_names=WALKER_WHOLE_BODY_JOINTS,
    forward_speed=0.3,
    walk_frequency=0.8,
    arm_swing_amplitude=0.35,
    ...
)
```

#### 场景对齐事件（[mdp/events.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/mdp/events.py)）

由于场景中 `robot` 与 `remote_robot` 启动时会被对齐到传送带，walker 也需要同样的对齐处理，否则会出现在远离主场景的初始世界坐标上：

```python
def align_walker_robot_to_conveyor(...):
    # 将 walker 放在 robot1 正后方 3.5 m，朝向传送带（+Y）
    walker_state[i, 0:2] = robot1_xy
    walker_state[i, 1] -= walker_y_behind         # 后退 3.5 m
    walker_state[i, 3:7] = (0.7071, 0, 0, 0.7071) # 90° 绕 Z 旋转 → 朝 +Y
```

### 后续扩展方向

将 `_sample_skeleton_pose` 替换为真实数据源：

| 数据源 | 替换方式 |
|--------|----------|
| ZMQ mocap 流 | 仿照 `G1TriHandUpperBodyZeroMqRetargeterCfg`，在 `__init__` 启动 SUB，每步从队列取最新 pose |
| BVH/FBX 文件 | `__init__` 加载文件 → 在 `_sample_skeleton_pose` 中按 phase 索引帧 |
| ALVR/SteamVR 全身追踪 | 通过 OpenXR Body Tracking 扩展拿到全身节点 → 重定向到 G1 关节 |
| SMPL 参数化模型 | 解码 SMPL pose 参数 → 关节角度 |

数据替换后，仅需保证 `_sample_skeleton_pose(phase) → 关节角度张量` 这个接口不变，其他逻辑（根节点平移、关节写入）无需改动。

---

## 相关文件索引

| 文件 | 说明 |
|------|------|
| [locomanipulation_g1_env_cfg.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/locomanipulation_g1_env_cfg.py) | 主环境配置；包含三个机器人定义和 walker 全身关节列表 |
| [configs/action_cfg.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/configs/action_cfg.py) | `AgileBasedLowerBodyActionCfg`、`AutoWalkActionCfg` 参数定义 |
| [mdp/actions.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/mdp/actions.py) | `AgileBasedLowerBodyAction`、`AutoWalkAction`（全身骨骼模拟）实现 |
| [mdp/events.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/mdp/events.py) | `place_robots_from_conveyor_bbox` / `align_walker_robot_to_conveyor` 等场景对齐事件 |
| [g1_head_pose_locomotion.py](../source/isaaclab_tasks/isaaclab_tasks/manager_based/locomanipulation/pick_place/g1_head_pose_locomotion.py) | 头显姿态 → 行走命令 retargeter（方案二备选） |
